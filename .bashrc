#######################################################
# ${HOME}/.bashrc: executed by bash(1) for non-login shells.
#######################################################

[ "${PS1-}" ] && source "${HOME}/.bash_profile";
